---
tags:
  - basic
  - easy
  - array
title: Left Rotate Array
author: Aarav Shah
status:
date: 21 May 2026
time: 11:05
Space Compl.: O(1)
Time Compl.: O(N)
---

-----
### Optimal Solution
###### Description
- Create a temp var that stores first index.
- Move elements 1 index behind
- store temp value at last index
---
```cpp
#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    void rotateArrayByOne(vector<int>& nums) {
        // Store the first element in a temporary variable
        int temp = nums[0];
        
        // Shift elements to the left
        for (int i = 1; i < nums.size(); ++i) {
            nums[i - 1] = nums[i];
        }

        // Place the first element at the end
        nums[nums.size() - 1] = temp;
    }
};

int main() {
    Solution solution;
    vector<int> nums = {1, 2, 3, 4, 5};

    solution.rotateArrayByOne(nums);

    for (int num : nums) {
        cout << num << " "; // Output the rotated array
    }

    return 0;
}
```
---
