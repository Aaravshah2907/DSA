---
tags:
  - basic
  - easy
  - array
title: Check if Sorted
author: Aarav Shah
status:
date: 21 May 2026
time: 11:05
Space Compl.: O(1)
Time Compl.: O(N)
---
-----
### Optimal Solution
###### Description
-> Check between current and next element.

---
```cpp
#include<bits/stdc++.h>
using namespace std;

// Function to check if the array is sorted
bool isSorted(int arr[], int n) {
    for (int i = 1; i < n; i++) {
        if (arr[i] < arr[i - 1])  // If any element is smaller than the previous one, return false
            return false;
    }

    return true;  // Return true if the array is sorted
}

int main() {
    int arr[] = {1, 2, 3, 4, 5}, n = 5;
    printf("%s", isSorted(arr, n) ? "True" : "False");  // Output result
}

```
---